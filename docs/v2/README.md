# PIS-Next V2 领域重构文档

文档状态：P04–P09 设计封版
文档版本：V2-0.2
创建日期：2026-08-08
设计原则：净室设计

## 1. 用途

本目录是 PIS-Next V2 Domain Reset 的独立设计基线。V2 以病理对象、患者安全、来源追溯、责任链、报告不可变和审计要求为业务依据，不把现有 P05 文档、任何旧 PIS、旧数据库或旧实现直接当作 V2 领域模型。

## 2. 文档顺序

| 文档 | 状态 | 用途 |
|---|---|---|
| `P00-current-system-audit.md` | 已完成 | 审计当前仓库、资产和旧语义 |
| `P01-domain-model.md` | 已完成 | 定义 V2 核心对象、关系和来源链 |
| `P02-domain-invariants.md` | 已完成 | 定义 V2 不变量和配置边界 |
| `P03-module-boundaries.md` | 已完成 | 定义模块所有权、依赖和隔离 |
| `P04-data-model.md` | 已完成（设计封版） | 将 V2 领域落为五层逻辑数据模型 |
| `P05-migration-plan.md` | 已完成（设计封版） | 基于当前 P15–P19 实现建立逐对象迁移映射和复核方案 |
| `P06-api-plan.md` | 已完成（设计封版） | 定义应用命令、查询、并发、幂等和外部接口边界 |
| `P07-frontend-plan.md` | 已完成（设计封版） | 定义角色工作台、病例上下文和诊断工作区体验 |
| `P08-test-plan.md` | 已完成（设计封版） | 定义不变量、架构、集成、权限和端到端测试 |
| `P09-cutover-plan.md` | 已完成（设计封版） | 定义 A–F 渐进替换、回滚和切换门禁 |
| `phase-1-change-summary.md` | 已完成 | 汇总本阶段新增、重构、淘汰、验证和风险 |
| `design-freeze-report.md` | 已完成 | 汇总 P04–P09 设计门禁、验证、阻断项和下一任务 |

## 3. 当前封版边界

P00–P09 前期设计已完成；本轮仍不修改现有 P15-P19 业务代码、数据库迁移、API、页面或生产切换。V2 模块目录仍作为隔离边界占位；当前工程实现属于待重构资产，不等于 V2 已完成。

下一任务固定为：V2-I01：Case / BusinessType / ApplicationItemMapping / PathologyNumberRule / Specimen 基础领域实现。进入 I01 前必须处理 P08 测试骨架和 P09 设计门禁；Docker Server 的历史阻断已解除，V2 运行时门禁仍按封版报告执行。

## 4. 规则优先级

V2 总指令和根目录 `AGENTS.md` 高于旧文档中的冲突语义。若后续发现 V2 基线本身存在业务不确定性，必须标记为“待业务确认”，不得用旧实现或猜测补齐。
