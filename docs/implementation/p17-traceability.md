# P17 双向追溯

| 正式基线 | 实现/证据 |
|---|---|
| `P09-REQ-0023` AC-01 | 有效处理结果 → 包埋要求快照 → 包埋事实 → 实际形成事实 → `tissue_block` ST-03；应用服务测试和 P17 smoke |
| `P09-REQ-0023` AC-02 | 缺来源、错误状态、无人工确认、版本冲突或无要求快照均拒绝；领域、H2、Web 和异常测试 |
| `SCN-PIS-030` | 处理任务、程序版本快照、批次、成员、运行、步骤和原始结果 |
| `SCN-PIS-031` | 有效结果人工确认、批次完成、部分成员影响与异常恢复 |
| `SCN-PIS-032` | 包埋任务、要求/方向、包埋事实、实际形成、替代和作废 |
| `P06-PROC-007` | S01–S06 由服务命令和状态守卫覆盖；D01–D03 由准入/结果/形成校验覆盖；E01–E03 由异常/恢复/返工覆盖 |
| `P07-EXC-019..021` | 异常表、成员影响、恢复、重处理和来源/版本拒绝错误 |
| `P14-PERM-050/014/029` | 控制器 → 应用服务 → 授权服务；组织范围和主体类型校验 |
| `P08-SM-004` | P16 稳定蜡块主身份复用；P17 只在实际形成时推进 ST-02 → ST-03，作废推进 ST-04 |

反向追溯链：前端命令 → `/api/p17` 路由 → `ProcessingApplicationService` → `JdbcProcessingRepository` → Flyway V5/V6 表 → audit/state history/outbox；所有核心变更均有幂等键和并发版本。
