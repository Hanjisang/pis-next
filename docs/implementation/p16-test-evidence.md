# P16 测试证据

本文件记录实际执行结果；未执行的检查不得填写为通过。

## 自动化覆盖

- 领域：批次状态、非法转换、取材事实不可变/更正、来源和数量、计划蜡块编号、标签版本/重打/作废。
- 应用：授权、范围、责任接管、同载荷幂等、异载荷冲突、乐观锁、审计和 outbox。
- JDBC：Flyway V1→V4、外键/唯一/检查约束、来源约束、并发编号和失败回滚。
- Web：独立队列、批次、取材、取样、计划蜡块、标签和完成接口测试。
- 前端：取材工作台和标签预览/提交行为。
- P15：既有登记、病例、标本接收、重复扫码、权限和范围回归。

## 实际结果

| 检查 | 结果 |
|---|---|
| `backend\\mvnw.cmd -B -ntp clean verify` | 通过；13 项测试，0 失败、0 错误、0 跳过 |
| 前端 `ci`、format/lint/typecheck/unit/build | 通过；Vitest 2 个测试文件、2 项测试 |
| Docker/Compose/Flyway/全栈烟雾 | 通过；Docker 29.6.2、Compose v5.3.1、PostgreSQL 18.4、Flyway V1～V4、后端 UP、前端 HTTP 200 |
| 静态禁用项、敏感信息、差异审查 | 通过；禁止框架/越界无实现匹配，打印匹配仅为安全提示，`git diff --check`通过 |

## 并发与失败证据

- 应用测试实际并发接管同一批次：仅一个请求成为权威责任人；并发建立计划蜡块：失败事务回滚，数据库保留一个有效计划；并发生成标签：同一蜡块只保留一个有效标签，另一请求返回 duplicate。
- 版本冲突使用数据库条件更新和预期版本；患者/病例/标本身份不一致返回 `P12-ERR-079` 并阻断；同幂等键异载荷返回 `P12-ERR-003`/HTTP 422。
- 合成 Docker 烟测实际完成登记、建案、预计标本、接收、P16 队列、接管、开始、取材记录、组织取样、计划蜡块、样本分配、标签生成、参考打印、重打和批次完成。
- 首轮 Docker 烟测发现幂等载荷冲突未映射为 Problem Details；已修复为应用层 `P12-ERR-003`，并在最终烟测复验通过。
- 统一脚本 `scripts/verify.ps1` 最终通过，验证结束后已执行 `docker compose --profile full down`，保留本地数据库卷。
