# P19 前端诊断与报告工作台

`P19DiagnosisReportWorkbench.vue` 提供诊断队列、任务接管、草稿、初诊提交、报告快照、审核和签发入口。页面只展示后端权威状态和版本；按钮可改善流程但不替代权限、增强认证或签发结论。签后版本显示为只读语义，补充、更正和撤回通过新业务命令进入版本链。

前端使用 `npm.cmd` 兼容 Windows 执行策略，P19 API 集中在 `frontend/src/api.ts`。本阶段不提供 PDF、打印、医院回传、患者端、数字切片阅读器或 AI 诊断。
