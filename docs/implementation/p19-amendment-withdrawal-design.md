# P19 补充、更正、撤回与重新签发

补充和更正均引用原有效版本并创建全新完整内容版本；原版本、原因、审核与签发历史保留。更正额外记录错误类型。撤回先记录申请和批准，再以增强认证执行撤回事实，把报告置于受控撤回状态；原内容和签发事实不可删除。重新签发只能绑定新内容版本，不能改写旧签发事实。

当前有效关系由 `p19_report.current_effective_version_id` 和追加关系记录共同表达。所有修订命令带幂等键；并发依靠报告/版本乐观锁、唯一约束和事务边界，冲突不采用静默 Last-Write-Wins。
