# PIS Next Repository Instructions

## 1. Project Mission

本仓库用于设计和开发一套生产级病理信息系统（Pathology Information System，PIS）。

本项目的首要目标依次为：

1. 患者安全；
2. 标本、蜡块、切片、数字切片和报告的全流程追溯；
3. 医疗数据完整性；
4. 报告版本不可篡改；
5. 权限、审计和责任追踪；
6. 系统可靠性；
7. 操作效率；
8. 界面完整性。

不得为了快速完成页面或功能而牺牲业务正确性、审计能力、数据完整性或自动化测试。

---

## 2. Working Language

项目业务文档、需求文档、测试场景和用户界面默认使用简体中文。

代码中的：

- 类名；
- 方法名；
- 变量名；
- 数据库对象名；
- API字段名；
- Git提交类型；

使用规范英文。

代码注释只在解释复杂业务规则、设计原因或非显而易见行为时使用。

---

## 3. Mandatory Working Process

接到任务后，必须按以下顺序工作：

1. 阅读本文件；
2. 阅读与任务相关的项目文档；
3. 检查当前Git状态；
4. 理解任务范围和验收条件；
5. 识别受影响的业务模块、数据和测试；
6. 对复杂任务先形成实施计划；
7. 实施修改；
8. 运行相关测试和静态检查；
9. 审查Git差异；
10. 修复发现的问题；
11. 更新相关文档；
12. 汇报完成内容、测试结果和剩余风险。

任务没有通过验证，不得仅因代码能够编译就声明完成。

---

## 4. Scope Control

必须严格执行当前提示词规定的任务范围。

除非当前任务明确要求，否则不得：

- 提前创建后续阶段文件；
- 提前开发业务代码；
- 擅自扩大功能范围；
- 擅自引入微服务；
- 擅自增加第三方依赖；
- 擅自改变技术栈；
- 擅自修改已确认的业务规则；
- 一次性生成大量未经验证的代码或文档。

发现后续需要处理的问题时，应记录为待办，不得在当前任务中顺带大规模实现。

## Clean-Room Design Policy

PIS Next 采用净室设计原则。

除非用户在具体任务中明确指定，否则不得：

- 导入旧 PIS 文档；
- 读取旧 PIS 源代码；
- 复制旧数据库结构；
- 复制旧接口状态定义；
- 复制旧系统模块划分；
- 根据旧页面反推新系统领域模型；
- 将历史实现认定为正确业务规则。

新系统的业务模型必须从病理对象、业务不变量、状态机、异常场景和追溯要求出发重新设计。

若未来需要参考旧系统，仅允许用于：

- 数据迁移字段映射；
- 上线差异核对；
- 已有功能查漏补缺；
- 历史数据兼容性分析。

参考旧系统时必须与新系统设计隔离，不得反向改变已经确认的新领域模型，除非经过明确业务评审。

---

## 5. Pathology Domain Principles

设计和开发过程中必须遵守以下病理业务原则：

1. 不得只使用一个病例状态表示全部病理流程状态。
2. 病例、标本、蜡块、切片、技术医嘱、数字切片和报告必须具有独立生命周期。
3. 病例展示状态应根据下级业务对象状态聚合得出。
4. 一个病理申请可以包含多个标本容器。
5. 一个病例可以包含多个标本。
6. 一个标本可以产生多个蜡块。
7. 一个蜡块可以产生多张不同用途的切片。
8. 普通切片必须能够追溯到来源蜡块和来源标本。
9. 外院蜡块或切片必须明确标记外部来源。
10. 冰冻剩余组织转入常规流程后，必须保留来源关系。
11. 补取材、重脱水、重包埋、重切、重染和重扫必须生成新业务记录。
12. 返工记录不得覆盖原始记录。
13. 已签发报告不得直接修改或覆盖。
14. 报告更正、补充、撤回和重新签发必须保留完整版本链。
15. 医疗业务记录不得物理删除。
16. 错误记录应通过作废、撤销、更正或失效处理。
17. 标签补打必须产生新的打印版本。
18. 新标签生效后，旧标签必须失效。
19. 关键业务状态不得由前端或控制器直接修改。
20. 所有关键操作必须产生审计记录。

---

## 6. Data Design Rules

数据库设计必须遵守：

1. 数据库内部ID与业务编号分离。
2. 内部ID必须不可变。
3. 病理号、标本号、蜡块号和切片号属于业务编号。
4. 核心实体之间应建立明确外键或可靠关联。
5. 不使用无法保证完整性的通用多态外键。
6. 不使用EAV模型承载核心病理业务数据。
7. 不使用无约束JSON代替核心结构化字段。
8. 所有表结构变更必须使用数据库迁移脚本。
9. 已执行的迁移脚本不得随意修改。
10. 必须设置必要的唯一约束、外键、检查约束和索引。
11. 所有重要业务对象必须支持乐观锁或等效并发控制。
12. 所有关键写操作必须具有明确事务边界。
13. 不得静默覆盖并发修改。
14. 医疗数据不得通过物理删除实现业务撤销。

---

## 7. Integration Rules

医院接口设计必须支持：

- 消息唯一标识；
- 幂等处理；
- 重复消息识别；
- 消息乱序处理；
- 自动重试；
- 失败补偿；
- 死信记录；
- 人工重放；
- 每日对账；
- 外部标识映射；
- 原始报文追溯；
- 敏感信息脱敏日志。

不得让外部系统接口直接修改核心业务表。

外部消息必须通过明确的应用命令进入业务领域。

---

## 8. Security and Privacy

必须遵守：

1. 只使用虚构数据、合成数据或经过批准的脱敏数据。
2. 不得在仓库中保存真实患者信息。
3. 不得在提示词、日志、测试代码或文档中保存真实患者信息。
4. 不得提交密码、Token、私钥、证书或生产环境连接信息。
5. 密钥必须通过环境变量或密钥管理机制注入。
6. `.env`等本地敏感配置必须进入`.gitignore`。
7. 日志必须避免输出患者敏感字段和认证信息。
8. 报告、数字切片和附件访问必须执行权限校验。
9. 所有特殊授权和高风险操作必须记录审计。

---

## 9. Architecture Rules

默认采用模块化单体架构。

除非有经过批准的架构决策记录，否则不得拆分微服务。

模块之间必须：

- 明确数据所有权；
- 不直接修改其他模块的数据表；
- 通过应用服务、领域事件或公开模块接口协作；
- 避免循环依赖；
- 保持控制器轻量；
- 将业务规则放在领域层或应用层；
- 将基础设施细节隔离在基础设施层。

不同医院的差异优先通过以下方式实现：

- 配置；
- 字典；
- 编号规则；
- 报告模板；
- 功能开关；
- 接口适配器。

不得为每家医院复制一套业务代码。

---

## 10. Testing Rules

每项业务功能必须根据风险配置相应测试。

至少考虑：

- 领域单元测试；
- 状态机测试；
- 数据库集成测试；
- API测试；
- 权限测试；
- 并发测试；
- 接口幂等测试；
- 前端测试；
- 端到端测试；
- 回归测试。

测试必须覆盖正常流程和异常流程。

不得：

- 删除失败测试来让构建通过；
- 随意跳过关键测试；
- 降低断言强度掩盖缺陷；
- 只测试页面是否显示；
- 只以覆盖率判断业务是否正确。

每个已修复缺陷应尽量增加对应的回归测试。

---

## 11. Git Rules

开始任务前检查：

```bash
git status
```

修改完成后必须：

1. 检查修改文件；
2. 审查差异；
3. 确认没有敏感数据；
4. 运行相关验证；
5. 再创建提交。

提交信息采用：

```text
<type>: <description>
```

常用类型：

- `chore`
- `docs`
- `feat`
- `fix`
- `test`
- `refactor`
- `build`
- `ci`
- `ops`

不得：

- 未经检查就提交大量生成文件；
- 覆盖他人有效修改；
- 使用强制推送；
- 重写已经共享的提交历史；
- 提交构建产物、日志、密钥或本地配置。

---

## 12. Documentation Rules

业务规则、数据库、接口和代码必须保持一致。

文档中的重要规则必须具有明确编号。

不确定的信息必须明确标记为：

```text
待业务确认
```

基于合理假设继续工作的内容必须记录：

- 假设内容；
- 选择原因；
- 可能影响；
- 后续确认方式。

不得把推测写成已经确认的业务事实。

不得编造：

- 测试结果；
- 性能结果；
- 安全认证；
- 医院接口联调结果；
- 生产部署结果；
- 用户验收结果。

---

## 13. Definition of Done

任务只有在同时满足以下条件时才算完成：

1. 当前任务范围内的文件或代码已经完成；
2. 业务规则与项目文档一致；
3. 相关测试已经运行；
4. 测试结果已经汇报；
5. Git差异已经审查；
6. 没有提交敏感信息；
7. 没有已知的阻断问题被隐藏；
8. 文档已同步更新；
9. 剩余风险和限制已明确说明。

若受环境限制无法完成某项验证，必须明确说明：

- 未完成的验证；
- 具体原因；
- 已完成的替代检查；
- 后续所需动作。

不得用“应该可以”“理论上可用”代替实际验证结果。
